@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h1>Stylists</h1>
<hr>
<br>
@if (@Model.Count == 0)
{
  <h3>No stylists have been added yet!</h3>
} 

<ul class="list-group">
  @foreach(Stylist Stylist in Model)
  {
    <li class="list-group-item">@Html.ActionLink($"✂  {Stylist.Name} ", "Details", new { id = Stylist.StylistId })</li>
  }
</ul>

<ul class="nav justify-content-center">
  <li class="nav-item">
    <a class="nav-link active">@Html.ActionLink("Add new stylist", "Create")</a>
  </li>
  <li class="nav-item">
    <a class="nav-link">@Html.ActionLink("Home", "Index", "Home")</a>
  </li>
</ul>